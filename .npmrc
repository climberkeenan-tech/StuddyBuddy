# Hoisted layout keeps Electron tooling (electron-builder, native rebuilds) happy.
node-linker=hoisted
shamefully-hoist=true
# Electron postinstall downloads the binary; allow its build script.
enable-pre-post-scripts=true
