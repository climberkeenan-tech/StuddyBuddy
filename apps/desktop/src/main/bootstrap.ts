/**
 * Service wiring for the main process: constructs the core runtime (storage,
 * AI, transcription, memory…) and registers the IPC router. Completed during
 * integration once core modules land.
 */
export async function bootstrap(): Promise<void> {
  // Placeholder — replaced by full wiring in the integration phase.
}
